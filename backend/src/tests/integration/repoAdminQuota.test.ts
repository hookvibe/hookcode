export {};

// NOTE:
// The OSS backend does not ship the "repo admin quota" feature. It may exist as an optional add-on in other builds.
// Keep this test file as a placeholder to avoid accidental re-introduction without proper wiring.

describe.skip('Repo admin quota (optional feature)', () => {
  test('skipped', () => {});
});
