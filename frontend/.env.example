# ==============================================================================
# HookCode Frontend - .env.example
# ==============================================================================
# Notes:
# - Vite only injects `VITE_*` variables at build time.
# - Non-`VITE_*` vars can still be used by Vite config (e.g. `HOOKCODE_FRONTEND_PORT`).
# - Copy to `frontend/.env` for local development, or provide it via Docker build args in production.
#
# 说明：
# - Vite 只会在构建阶段注入 `VITE_*` 变量。
# - 非 `VITE_*` 变量仍可被 Vite 配置读取（例如：`HOOKCODE_FRONTEND_PORT`）。
# - 本文件可复制为 `frontend/.env`（本地开发）；生产部署请通过 Docker build args 注入。

# Frontend dev server port (Vite) (default: 5173)
# 前端开发服务端口（Vite）（默认：5173）
HOOKCODE_FRONTEND_PORT=5173

# Add HOOKCODE_BACKEND_PORT to keep Vite `/api` proxy aligned with the local backend port. na4jfazi3z1vsrfofcj1
# Backend port for Vite `/api/*` proxy when using a relative API base (optional; default: 4000)
# 当使用相对 API Base（例如 `/api`）时，Vite `/api/*` 代理使用的后端端口（可选；默认：4000）
HOOKCODE_BACKEND_PORT=4000

# Backend API base URL used by the frontend (must end with `/api`)
# 前端请求后端 API 的基地址（必须以 `/api` 结尾）
# Prefer 127.0.0.1 over localhost to avoid IPv6 (::1) resolution pitfalls in local dev. na4jfazi3z1vsrfofcj1
# Example (prod): http://127.0.0.1:4000/api
# 示例（生产）：http://127.0.0.1:4000/api
VITE_API_BASE_URL=http://127.0.0.1:4000/api

# Disable account display-name/password editing in the user panel (optional; default: false)
# 是否禁用用户面板中的账号显示名/密码编辑功能（可选；默认：false）
VITE_DISABLE_ACCOUNT_EDIT=false
